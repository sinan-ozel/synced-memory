{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"\ud83d\uddc4\ufe0f synced-memory","text":"<p>A production-ready Python class for seamless, multiprocessing-safe, persistent key-value storage using Redis or DragonflyDB as a backend. If the backend is unavailable, values are cached locally and queued for syncing when it comes back online. All values are serialized as JSON, and you interact with it using natural Python attribute access.</p>"},{"location":"#purpose","title":"Purpose","text":"<p>The intention is to use this with agentic workflows deployed as microservices, allowing for multiple instances of the same pod to share their state.</p>"},{"location":"#features","title":"\u2728 Features","text":"<ul> <li>\ud83d\udd04 Multiprocessing-safe: All processes share the same state via Redis or DragonflyDB.</li> <li>\ud83e\udde0 Pythonic API: Set and get attributes as if they were regular object properties.</li> <li>\ud83d\udd70\ufe0f Persistence: Values survive process restarts and context blocks.</li> <li>\ud83d\udea6 Resilient: If the backend is down, changes are queued and flushed when it returns.</li> <li>\ud83e\udde9 Customizable: Prefixes and conversation IDs for namespacing.</li> <li>\ud83e\uddf5 Background sync: Queued changes are flushed automatically in the background.</li> </ul>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>from synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nmem = Memory()\nmem.answer = 42\nprint(mem.answer)  # 42\n\n# Across processes or instances:\nmem2 = Memory()\nprint(mem2.answer)  # 42\n\nmem.settings = {\"theme\": \"dark\", \"volume\": 0.75}\nprint(mem.settings)  # {'theme': 'dark', 'volume': 0.75}\n</code></pre>"},{"location":"#getting-started","title":"Getting Started","text":"<p>Check out the Installation Guide and Quickstart Tutorial to get up and running.</p>"},{"location":"#license","title":"License","text":"<p>MIT</p>"},{"location":"api/reference/","title":"API Reference","text":""},{"location":"api/reference/#base-classes","title":"Base Classes","text":""},{"location":"api/reference/#memorybase","title":"MemoryBase","text":""},{"location":"api/reference/#synced_memory.common.MemoryBase","title":"<code>synced_memory.common.MemoryBase</code>","text":"<p>A synchronized key-value store backed by a Redis-compatible server.</p> <p>If the backend is unavailable, values are cached locally and queued for syncing when it comes back online.</p>"},{"location":"api/reference/#synced_memory.common.MemoryBase--environment-variables","title":"Environment Variables:","text":"<ul> <li>REDIS_HOST:  Hostname of the backend server</li> <li>REDIS_PORT:  Port of the backend server</li> </ul>"},{"location":"api/reference/#synced_memory.common.MemoryBase--attributes","title":"Attributes:","text":"<p>_timeout : float     Timeout for backend operations in seconds (default: 0.5). _queue : list     Queue of (key, value) tuples to be synced when backend is available. _attributes : dict     Local cache of attributes.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>class MemoryBase:\n    \"\"\"A synchronized key-value store backed by a Redis-compatible server.\n\n    If the backend is unavailable, values are cached locally and queued for\n    syncing when it comes back online.\n\n    Environment Variables:\n    ----------------------\n    - REDIS_HOST:  Hostname of the backend server\n    - REDIS_PORT:  Port of the backend server\n\n    Attributes:\n    -----------\n    _timeout : float\n        Timeout for backend operations in seconds (default: 0.5).\n    _queue : list\n        Queue of (key, value) tuples to be synced when backend is available.\n    _attributes : dict\n        Local cache of attributes.\n    \"\"\"\n\n    def __init__(\n        self,\n        backend_hostname: str = \"redis\",\n        backend_port: int = 6379,\n        backend_prefix: str = \"memory:\",\n    ):\n        \"\"\"Initialize the MemoryBase instance and flush any queued updates.\"\"\"\n        self._host = os.environ.get(\"REDIS_HOST\", backend_hostname)\n        self._port = int(os.environ.get(\"REDIS_PORT\", backend_port))\n        self._prefix = backend_prefix\n        self._timeout = 0.5  # Seconds\n\n        self._queue = []\n        self._attributes = {}\n        self._last_modified = {}\n\n        self._stop_event = threading.Event()\n        self._thread = None\n\n        self._redis_available = False\n        self._is_connected_to_redis_at_least_once = False\n\n        self.start_background_flush()\n        self._load_from_redis()\n\n    def __enter__(self):\n        \"\"\"Enter the runtime context related to this object.\"\"\"\n        return self\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        \"\"\"Flush any remaining queue then stop the background thread.\"\"\"\n        if self._queue:\n            while True:\n                try:\n                    self._flush_queue()\n                    break\n                except Exception:\n                    if not self._is_connected_to_redis_at_least_once:\n                        break\n                    time.sleep(1)\n\n        self.stop_background_flush()\n\n    def _connect(self):\n        \"\"\"Establish a new backend connection.\n\n        Returns:\n            redis.Redis: A client if connection works; raises on failure.\n        \"\"\"\n        import redis\n\n        client = redis.Redis(\n            host=self._host,\n            port=self._port,\n            socket_connect_timeout=self._timeout,\n            socket_timeout=self._timeout,\n        )\n        client.ping()\n        return client\n\n    def _key(self, name):\n        \"\"\"Generate backend key with prefix.\"\"\"\n        return f\"{self._prefix}{name}\"\n\n    def _flush_queue(self):\n        \"\"\"Attempt one flush of the queue to the backend.\n\n        Connects once; raises on failure so the caller can apply backoff. For\n        each queued item, skips writes where the backend already holds a newer\n        timestamp.\n        \"\"\"\n        client = self._connect()\n        self._is_connected_to_redis_at_least_once = True\n\n        while self._queue:\n            key, payload = self._queue.pop(0)\n            value = payload.get(\"value\")\n            queued_timestamp = payload.get(\"last_modified\")\n\n            raw = client.get(self._key(key))\n            redis_timestamp = 0\n            if raw is not None:\n                obj = json.loads(raw)\n                redis_timestamp = obj.get(\"last_modified\", 0)\n\n            if redis_timestamp &gt; queued_timestamp:\n                continue\n\n            if value is None:\n                client.delete(self._key(key))\n            else:\n                client.set(self._key(key), json.dumps(payload))\n\n    def _background_flush_loop(self):\n        \"\"\"Own the backend connection state and flush the write queue.\n\n        On each cycle this thread attempts to connect and flush any queued\n        writes. Success sets _redis_available = True and resets the backoff to\n        1 s. Failure sets _redis_available = False and doubles the wait (capped\n        at 30 s), so the main thread can fall back to local cache without\n        blocking.\n        \"\"\"\n        backoff = 1\n        while not self._stop_event.is_set():\n            try:\n                self._flush_queue()\n                self._redis_available = True\n                backoff = 1\n            except Exception:\n                self._redis_available = False\n                backoff = min(backoff * 2, 30)\n                self._stop_event.wait(backoff)\n                continue\n            self._stop_event.wait(1)\n\n    def start_background_flush(self):\n        \"\"\"Start the background thread to flush the queue regularly.\"\"\"\n        if self._thread is None or not self._thread.is_alive():\n            self._stop_event.clear()\n            self._thread = threading.Thread(\n                target=self._background_flush_loop, daemon=True\n            )\n            self._thread.start()\n\n    def stop_background_flush(self):\n        \"\"\"Stop the background flushing thread.\"\"\"\n        self._stop_event.set()\n        if self._thread is not None:\n            self._thread.join()\n            self._thread = None\n\n    def _load_from_redis(self):\n        \"\"\"Load all keys with the current prefix from the backend into local\n        cache.\"\"\"\n        try:\n            client = self._connect()\n            self._redis_available = True\n            self._is_connected_to_redis_at_least_once = True\n            pattern = f\"{self._prefix}*\"\n            for key in client.scan_iter(match=pattern):\n                name = key.decode().replace(self._prefix, \"\", 1)\n                try:\n                    raw = client.get(key)\n                    obj = json.loads(raw)\n                    if (\n                        isinstance(obj, dict)\n                        and \"value\" in obj\n                        and \"last_modified\" in obj\n                    ):\n                        self._attributes[name] = obj[\"value\"]\n                        self._last_modified[name] = obj[\"last_modified\"]\n                    else:\n                        self._attributes[name] = obj\n                except Exception:\n                    pass\n        except Exception:\n            self._redis_available = False\n\n    def __setattr__(self, name, value):\n        \"\"\"Set an attribute.\n\n        Store in backend if available, otherwise queue it.\n                Raises:\n                    TypeError: If the value is not JSON-serializable.\n        \"\"\"\n        if name.startswith(\"_\"):\n            super().__setattr__(name, value)\n            return\n\n        try:\n            _ = json.dumps(value)\n        except (TypeError, ValueError):\n            raise TypeError(\n                f\"Value for '{name}' is not JSON-serializable: {type(value)}\"\n            )\n\n        self._set(name, value)\n\n    def _write_to_redis_or_queue(self, name: str, payload: dict):\n        \"\"\"Write to backend if available; queue for later if not.\"\"\"\n        if self._redis_available:\n            try:\n                client = self._connect()\n                client.set(self._key(name), json.dumps(payload))\n                self._is_connected_to_redis_at_least_once = True\n                return\n            except Exception:\n                self._redis_available = False\n        self._queue.append((name, payload))\n\n    def _set(self, name: str, value: Any):\n        \"\"\"Update local cache and write through to backend (or queue if\n        down).\"\"\"\n        self._attributes[name] = wrap_sync(value, self, name)\n        timestamp = time.time_ns()\n        self._last_modified[name] = timestamp\n        self._write_to_redis_or_queue(\n            name, {\"value\": value, \"last_modified\": timestamp}\n        )\n\n    def __getattr__(self, name: str) -&gt; Any:\n        \"\"\"Return the attribute, reading from backend when available.\n\n        Raises:\n            AttributeError: If the attribute has not been set.\n        \"\"\"\n        if name.startswith(\"_\"):\n            return super().__getattribute__(name)\n\n        if self._redis_available:\n            try:\n                client = self._connect()\n                raw = client.get(self._key(name))\n                if raw is not None:\n                    self._is_connected_to_redis_at_least_once = True\n                    obj = json.loads(raw)\n                    value = obj[\"value\"]\n                    self._attributes[name] = wrap_sync(value, self, name)\n                    self._last_modified[name] = obj[\"last_modified\"]\n                    return self._attributes[name]\n            except Exception:\n                self._redis_available = False\n\n        if name in self._attributes:\n            return self._attributes[name]\n        raise AttributeError(f\"'Memory' object has no attribute '{name}'\")\n\n    def sync(self, name: str):\n        \"\"\"Write the current local value of an attribute to backend (or queue\n        if down).\"\"\"\n        if name not in self._attributes:\n            raise AttributeError(f\"'Memory' object has no attribute '{name}'\")\n\n        self._write_to_redis_or_queue(\n            name,\n            {\n                \"value\": self._attributes[name],\n                \"last_modified\": self._last_modified.get(name, 0),\n            },\n        )\n\n    def __delattr__(self, name):\n        \"\"\"Remove an attribute from local cache and queue its deletion.\n\n        Raises:\n            AttributeError: If the attribute is not found.\n        \"\"\"\n        if name.startswith(\"_\"):\n            super().__delattr__(name)\n            return\n\n        if name not in self._attributes:\n            raise AttributeError(f\"'Memory' object has no attribute '{name}'\")\n\n        del self._attributes[name]\n        if name in self._last_modified:\n            del self._last_modified[name]\n\n        timestamp = time.time_ns()\n        if self._redis_available:\n            try:\n                client = self._connect()\n                client.delete(self._key(name))\n                self._is_connected_to_redis_at_least_once = True\n                return\n            except Exception:\n                self._redis_available = False\n        self._queue.append((name, {\"value\": None, \"last_modified\": timestamp}))\n</code></pre>"},{"location":"api/reference/#synced_memory.common.MemoryBase.__delattr__","title":"<code>__delattr__(name)</code>","text":"<p>Remove an attribute from local cache and queue its deletion.</p> <p>Raises:</p> Type Description <code>AttributeError</code> <p>If the attribute is not found.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def __delattr__(self, name):\n    \"\"\"Remove an attribute from local cache and queue its deletion.\n\n    Raises:\n        AttributeError: If the attribute is not found.\n    \"\"\"\n    if name.startswith(\"_\"):\n        super().__delattr__(name)\n        return\n\n    if name not in self._attributes:\n        raise AttributeError(f\"'Memory' object has no attribute '{name}'\")\n\n    del self._attributes[name]\n    if name in self._last_modified:\n        del self._last_modified[name]\n\n    timestamp = time.time_ns()\n    if self._redis_available:\n        try:\n            client = self._connect()\n            client.delete(self._key(name))\n            self._is_connected_to_redis_at_least_once = True\n            return\n        except Exception:\n            self._redis_available = False\n    self._queue.append((name, {\"value\": None, \"last_modified\": timestamp}))\n</code></pre>"},{"location":"api/reference/#synced_memory.common.MemoryBase.__enter__","title":"<code>__enter__()</code>","text":"<p>Enter the runtime context related to this object.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def __enter__(self):\n    \"\"\"Enter the runtime context related to this object.\"\"\"\n    return self\n</code></pre>"},{"location":"api/reference/#synced_memory.common.MemoryBase.__exit__","title":"<code>__exit__(exc_type, exc_val, exc_tb)</code>","text":"<p>Flush any remaining queue then stop the background thread.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def __exit__(self, exc_type, exc_val, exc_tb):\n    \"\"\"Flush any remaining queue then stop the background thread.\"\"\"\n    if self._queue:\n        while True:\n            try:\n                self._flush_queue()\n                break\n            except Exception:\n                if not self._is_connected_to_redis_at_least_once:\n                    break\n                time.sleep(1)\n\n    self.stop_background_flush()\n</code></pre>"},{"location":"api/reference/#synced_memory.common.MemoryBase.__getattr__","title":"<code>__getattr__(name)</code>","text":"<p>Return the attribute, reading from backend when available.</p> <p>Raises:</p> Type Description <code>AttributeError</code> <p>If the attribute has not been set.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def __getattr__(self, name: str) -&gt; Any:\n    \"\"\"Return the attribute, reading from backend when available.\n\n    Raises:\n        AttributeError: If the attribute has not been set.\n    \"\"\"\n    if name.startswith(\"_\"):\n        return super().__getattribute__(name)\n\n    if self._redis_available:\n        try:\n            client = self._connect()\n            raw = client.get(self._key(name))\n            if raw is not None:\n                self._is_connected_to_redis_at_least_once = True\n                obj = json.loads(raw)\n                value = obj[\"value\"]\n                self._attributes[name] = wrap_sync(value, self, name)\n                self._last_modified[name] = obj[\"last_modified\"]\n                return self._attributes[name]\n        except Exception:\n            self._redis_available = False\n\n    if name in self._attributes:\n        return self._attributes[name]\n    raise AttributeError(f\"'Memory' object has no attribute '{name}'\")\n</code></pre>"},{"location":"api/reference/#synced_memory.common.MemoryBase.__init__","title":"<code>__init__(backend_hostname='redis', backend_port=6379, backend_prefix='memory:')</code>","text":"<p>Initialize the MemoryBase instance and flush any queued updates.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def __init__(\n    self,\n    backend_hostname: str = \"redis\",\n    backend_port: int = 6379,\n    backend_prefix: str = \"memory:\",\n):\n    \"\"\"Initialize the MemoryBase instance and flush any queued updates.\"\"\"\n    self._host = os.environ.get(\"REDIS_HOST\", backend_hostname)\n    self._port = int(os.environ.get(\"REDIS_PORT\", backend_port))\n    self._prefix = backend_prefix\n    self._timeout = 0.5  # Seconds\n\n    self._queue = []\n    self._attributes = {}\n    self._last_modified = {}\n\n    self._stop_event = threading.Event()\n    self._thread = None\n\n    self._redis_available = False\n    self._is_connected_to_redis_at_least_once = False\n\n    self.start_background_flush()\n    self._load_from_redis()\n</code></pre>"},{"location":"api/reference/#synced_memory.common.MemoryBase.__setattr__","title":"<code>__setattr__(name, value)</code>","text":"<p>Set an attribute.</p> <p>Store in backend if available, otherwise queue it.         Raises:             TypeError: If the value is not JSON-serializable.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def __setattr__(self, name, value):\n    \"\"\"Set an attribute.\n\n    Store in backend if available, otherwise queue it.\n            Raises:\n                TypeError: If the value is not JSON-serializable.\n    \"\"\"\n    if name.startswith(\"_\"):\n        super().__setattr__(name, value)\n        return\n\n    try:\n        _ = json.dumps(value)\n    except (TypeError, ValueError):\n        raise TypeError(\n            f\"Value for '{name}' is not JSON-serializable: {type(value)}\"\n        )\n\n    self._set(name, value)\n</code></pre>"},{"location":"api/reference/#synced_memory.common.MemoryBase.start_background_flush","title":"<code>start_background_flush()</code>","text":"<p>Start the background thread to flush the queue regularly.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def start_background_flush(self):\n    \"\"\"Start the background thread to flush the queue regularly.\"\"\"\n    if self._thread is None or not self._thread.is_alive():\n        self._stop_event.clear()\n        self._thread = threading.Thread(\n            target=self._background_flush_loop, daemon=True\n        )\n        self._thread.start()\n</code></pre>"},{"location":"api/reference/#synced_memory.common.MemoryBase.stop_background_flush","title":"<code>stop_background_flush()</code>","text":"<p>Stop the background flushing thread.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def stop_background_flush(self):\n    \"\"\"Stop the background flushing thread.\"\"\"\n    self._stop_event.set()\n    if self._thread is not None:\n        self._thread.join()\n        self._thread = None\n</code></pre>"},{"location":"api/reference/#synced_memory.common.MemoryBase.sync","title":"<code>sync(name)</code>","text":"<p>Write the current local value of an attribute to backend (or queue if down).</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def sync(self, name: str):\n    \"\"\"Write the current local value of an attribute to backend (or queue\n    if down).\"\"\"\n    if name not in self._attributes:\n        raise AttributeError(f\"'Memory' object has no attribute '{name}'\")\n\n    self._write_to_redis_or_queue(\n        name,\n        {\n            \"value\": self._attributes[name],\n            \"last_modified\": self._last_modified.get(name, 0),\n        },\n    )\n</code></pre>"},{"location":"api/reference/#prefixedmemorybase","title":"PrefixedMemoryBase","text":""},{"location":"api/reference/#synced_memory.common.PrefixedMemoryBase","title":"<code>synced_memory.common.PrefixedMemoryBase</code>","text":"<p>               Bases: <code>MemoryBase</code></p> <p>MemoryBase subclass that namespaces keys by a scope prefix.</p> <p>Parameters:</p> Name Type Description Default <code>prefix</code> <code>str</code> <p>Unique prefix to isolate this scope's memory.</p> required <code>backend_hostname</code> <code>str</code> <p>Backend host.</p> <code>'redis'</code> <code>backend_port</code> <code>int</code> <p>Backend port.</p> <code>6379</code> <code>backend_prefix</code> <code>str</code> <p>Base key prefix.</p> <code>'memory:'</code> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>class PrefixedMemoryBase(MemoryBase):\n    \"\"\"MemoryBase subclass that namespaces keys by a scope prefix.\n\n    Args:\n        prefix (str): Unique prefix to isolate this scope's memory.\n        backend_hostname (str): Backend host.\n        backend_port (int): Backend port.\n        backend_prefix (str): Base key prefix.\n    \"\"\"\n\n    def __init__(\n        self,\n        prefix: str,\n        backend_hostname: str = \"redis\",\n        backend_port: int = 6379,\n        backend_prefix: str = \"memory:\",\n    ):\n        self._scope = prefix\n        super().__init__(\n            backend_hostname=backend_hostname,\n            backend_port=backend_port,\n            backend_prefix=backend_prefix,\n        )\n\n    def _key(self, name):\n        return f\"{self._prefix}{self._scope}:{name}\"\n</code></pre>"},{"location":"api/reference/#redis-backend","title":"Redis Backend","text":""},{"location":"api/reference/#memory","title":"Memory","text":""},{"location":"api/reference/#synced_memory.redis.Memory","title":"<code>synced_memory.redis.Memory</code>","text":"<p>               Bases: <code>MemoryBase</code></p> <p>A synchronized key-value store backed by Redis.</p> <p>See MemoryBase for full documentation.</p> Source code in <code>synced_memory/redis/__init__.py</code> <pre><code>class Memory(MemoryBase):\n    \"\"\"A synchronized key-value store backed by Redis.\n\n    See MemoryBase for full documentation.\n    \"\"\"\n\n    def __init__(\n        self,\n        redis_hostname: str = \"redis\",\n        redis_port: int = 6379,\n        db_prefix: str = \"memory:\",\n        redis_prefix: str | None = None,\n        dragonflydb_prefix: str | None = None,\n    ):\n        effective = redis_prefix or dragonflydb_prefix or db_prefix\n        super().__init__(\n            backend_hostname=redis_hostname,\n            backend_port=redis_port,\n            backend_prefix=effective,\n        )\n</code></pre>"},{"location":"api/reference/#prefixedmemory","title":"PrefixedMemory","text":""},{"location":"api/reference/#synced_memory.redis.PrefixedMemory","title":"<code>synced_memory.redis.PrefixedMemory</code>","text":"<p>               Bases: <code>PrefixedMemoryBase</code></p> <p>Prefixed memory backed by Redis.</p> <p>Namespaces all keys by the given prefix scope.</p> Source code in <code>synced_memory/redis/__init__.py</code> <pre><code>class PrefixedMemory(PrefixedMemoryBase):\n    \"\"\"Prefixed memory backed by Redis.\n\n    Namespaces all keys by the given prefix scope.\n    \"\"\"\n\n    def __init__(\n        self,\n        prefix: str,\n        redis_hostname: str = \"redis\",\n        redis_port: int = 6379,\n        db_prefix: str = \"memory:\",\n        redis_prefix: str | None = None,\n        dragonflydb_prefix: str | None = None,\n    ):\n        effective = redis_prefix or dragonflydb_prefix or db_prefix\n        super().__init__(\n            prefix=prefix,\n            backend_hostname=redis_hostname,\n            backend_port=redis_port,\n            backend_prefix=effective,\n        )\n</code></pre>"},{"location":"api/reference/#dragonflydb-backend","title":"DragonflyDB Backend","text":""},{"location":"api/reference/#memory_1","title":"Memory","text":""},{"location":"api/reference/#synced_memory.dragonflydb.Memory","title":"<code>synced_memory.dragonflydb.Memory</code>","text":"<p>               Bases: <code>MemoryBase</code></p> <p>A synchronized key-value store backed by DragonflyDB.</p> <p>See MemoryBase for full documentation.</p> Source code in <code>synced_memory/dragonflydb/__init__.py</code> <pre><code>class Memory(MemoryBase):\n    \"\"\"A synchronized key-value store backed by DragonflyDB.\n\n    See MemoryBase for full documentation.\n    \"\"\"\n\n    def __init__(\n        self,\n        dragonflydb_hostname: str = \"dragonflydb\",\n        dragonflydb_port: int = 6379,\n        db_prefix: str = \"memory:\",\n        redis_prefix: str | None = None,\n        dragonflydb_prefix: str | None = None,\n    ):\n        effective = redis_prefix or dragonflydb_prefix or db_prefix\n        super().__init__(\n            backend_hostname=dragonflydb_hostname,\n            backend_port=dragonflydb_port,\n            backend_prefix=effective,\n        )\n</code></pre>"},{"location":"api/reference/#prefixedmemory_1","title":"PrefixedMemory","text":""},{"location":"api/reference/#synced_memory.dragonflydb.PrefixedMemory","title":"<code>synced_memory.dragonflydb.PrefixedMemory</code>","text":"<p>               Bases: <code>PrefixedMemoryBase</code></p> <p>Prefixed memory backed by DragonflyDB.</p> <p>Namespaces all keys by the given prefix scope.</p> Source code in <code>synced_memory/dragonflydb/__init__.py</code> <pre><code>class PrefixedMemory(PrefixedMemoryBase):\n    \"\"\"Prefixed memory backed by DragonflyDB.\n\n    Namespaces all keys by the given prefix scope.\n    \"\"\"\n\n    def __init__(\n        self,\n        prefix: str,\n        dragonflydb_hostname: str = \"dragonflydb\",\n        dragonflydb_port: int = 6379,\n        db_prefix: str = \"memory:\",\n        redis_prefix: str | None = None,\n        dragonflydb_prefix: str | None = None,\n    ):\n        effective = redis_prefix or dragonflydb_prefix or db_prefix\n        super().__init__(\n            prefix=prefix,\n            backend_hostname=dragonflydb_hostname,\n            backend_port=dragonflydb_port,\n            backend_prefix=effective,\n        )\n</code></pre>"},{"location":"api/reference/#helpers","title":"Helpers","text":""},{"location":"api/reference/#syncedlist","title":"SyncedList","text":""},{"location":"api/reference/#synced_memory.common.SyncedList","title":"<code>synced_memory.common.SyncedList</code>","text":"<p>               Bases: <code>list</code></p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>class SyncedList(list):\n\n    def __init__(self, iterable, parent, topmost_key: str):\n        for i, item in enumerate(iterable):\n            iterable[i] = wrap_sync(item, self, f\"{topmost_key}[{i}]\")\n        super().__init__(iterable)\n        self._parent = parent\n        self._topmost_key = topmost_key\n\n    def append(self, item):\n        super().append(item)\n        self._parent.sync(self._topmost_key)\n\n    def extend(self, iterable):\n        super().extend(iterable)\n        self._parent.sync(self._topmost_key)\n\n    def insert(self, index, item):\n        super().insert(index, item)\n        self._parent.sync(self._topmost_key)\n\n    def remove(self, item):\n        super().remove(item)\n        self._parent.sync(self._topmost_key)\n\n    def pop(self, index=-1):\n        item = super().pop(index)\n        self._parent.sync(self._topmost_key)\n        return item\n\n    def sync(self, name: str):\n        self._parent.sync(self._topmost_key)\n\n    def aslist(self):\n        \"\"\"Return a plain Python list, recursively converting nested\n        SyncedList/SyncedDict objects.\"\"\"\n        result = []\n        for item in self:\n            if isinstance(item, SyncedList):\n                result.append(item.aslist())\n            elif isinstance(item, SyncedDict):\n                result.append(item.asdict())\n            else:\n                result.append(item)\n        return result\n</code></pre>"},{"location":"api/reference/#synced_memory.common.SyncedList.aslist","title":"<code>aslist()</code>","text":"<p>Return a plain Python list, recursively converting nested SyncedList/SyncedDict objects.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def aslist(self):\n    \"\"\"Return a plain Python list, recursively converting nested\n    SyncedList/SyncedDict objects.\"\"\"\n    result = []\n    for item in self:\n        if isinstance(item, SyncedList):\n            result.append(item.aslist())\n        elif isinstance(item, SyncedDict):\n            result.append(item.asdict())\n        else:\n            result.append(item)\n    return result\n</code></pre>"},{"location":"api/reference/#synceddict","title":"SyncedDict","text":""},{"location":"api/reference/#synced_memory.common.SyncedDict","title":"<code>synced_memory.common.SyncedDict</code>","text":"<p>               Bases: <code>dict</code></p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>class SyncedDict(dict):\n\n    def __init__(self, mapping, parent, topmost_key: str):\n        for k, v in mapping.items():\n            mapping[k] = wrap_sync(v, self, topmost_key)\n        super().__init__(mapping)\n        self._parent = parent\n        self._topmost_key = topmost_key\n\n    def __setitem__(self, k, v):\n        super().__setitem__(k, v)\n        self._parent.sync(self._topmost_key)\n\n    def update(self, *args, **kwargs):\n        super().update(*args, **kwargs)\n        self._parent.sync(self._topmost_key)\n\n    def pop(self, key, *args):\n        item = super().pop(key, *args)\n        self._parent.sync(self._topmost_key)\n        return item\n\n    def sync(self, name: str):\n        self._parent.sync(self._topmost_key)\n\n    def asdict(self):\n        \"\"\"Return a plain Python dict, recursively converting nested\n        SyncedList/SyncedDict objects.\"\"\"\n        result = {}\n        for key, value in self.items():\n            if isinstance(value, SyncedList):\n                result[key] = value.aslist()\n            elif isinstance(value, SyncedDict):\n                result[key] = value.asdict()\n            else:\n                result[key] = value\n        return result\n</code></pre>"},{"location":"api/reference/#synced_memory.common.SyncedDict.asdict","title":"<code>asdict()</code>","text":"<p>Return a plain Python dict, recursively converting nested SyncedList/SyncedDict objects.</p> Source code in <code>synced_memory/common/__init__.py</code> <pre><code>def asdict(self):\n    \"\"\"Return a plain Python dict, recursively converting nested\n    SyncedList/SyncedDict objects.\"\"\"\n    result = {}\n    for key, value in self.items():\n        if isinstance(value, SyncedList):\n            result[key] = value.aslist()\n        elif isinstance(value, SyncedDict):\n            result[key] = value.asdict()\n        else:\n            result[key] = value\n    return result\n</code></pre>"},{"location":"api/reference/#environment-variables","title":"Environment Variables","text":"Variable Default Description <code>REDIS_HOST</code> <code>redis</code> Backend hostname <code>REDIS_PORT</code> <code>6379</code> Backend port <code>REDIS_PREFIX</code> <code>memory:</code> Key prefix for namespacing"},{"location":"api/reference/#exceptions","title":"Exceptions","text":"<p>synced-memory uses standard Python exceptions:</p> <ul> <li><code>AttributeError</code>: Raised when accessing non-existent attributes</li> <li><code>TypeError</code>: Raised when assigning a non-JSON-serializable value</li> <li><code>redis.ConnectionError</code>: Raised when the backend connection fails (if not caught by the circuit breaker)</li> </ul>"},{"location":"development/contributing/","title":"Contributing","text":"<p>Thank you for your interest in contributing to synced-memory!</p>"},{"location":"development/contributing/#getting-started","title":"Getting Started","text":""},{"location":"development/contributing/#prerequisites","title":"Prerequisites","text":"<ul> <li>Docker (required)</li> <li>Git</li> <li>VS Code (optional, recommended for devcontainer support)</li> </ul>"},{"location":"development/contributing/#fork-and-clone","title":"Fork and Clone","text":"<pre><code>git clone https://github.com/sinan-ozel/synced-memory.git\ncd synced-memory\n</code></pre>"},{"location":"development/contributing/#development-environment","title":"Development Environment","text":""},{"location":"development/contributing/#option-1-vs-code-dev-containers-recommended","title":"Option 1: VS Code Dev Containers (Recommended)","text":"<ol> <li>Open the project in VS Code</li> <li>Install the \"Dev Containers\" extension</li> <li>Press <code>Cmd/Ctrl + Shift + P</code> \u2192 \"Dev Containers: Reopen in Container\"</li> </ol>"},{"location":"development/contributing/#option-2-docker-compose","title":"Option 2: Docker Compose","text":"<pre><code>docker compose -f tests/docker-compose.yaml up --build --abort-on-container-exit --exit-code-from test\n</code></pre>"},{"location":"development/contributing/#option-3-local-virtual-environment","title":"Option 3: Local Virtual Environment","text":"<pre><code>python -m venv .venv\nsource .venv/bin/activate\n\npip install --upgrade pip\npip install -e .\npip install -e .[test]\npip install -e .[dev]\npip install -e .[docs]\n</code></pre>"},{"location":"development/contributing/#development-workflow","title":"Development Workflow","text":""},{"location":"development/contributing/#1-create-a-branch","title":"1. Create a Branch","text":"<pre><code>git checkout -b feature/your-feature-name\n</code></pre> <p>Use prefixes: <code>feature/</code>, <code>bugfix/</code>, <code>docs/</code>, <code>refactor/</code></p>"},{"location":"development/contributing/#2-run-tests","title":"2. Run Tests","text":"<pre><code>docker compose -f tests/docker-compose.yaml up --build --abort-on-container-exit --exit-code-from test\n</code></pre>"},{"location":"development/contributing/#3-format-and-lint","title":"3. Format and Lint","text":"<pre><code>docker compose -f reformat/docker-compose.yaml up --build --abort-on-container-exit\ndocker compose -f lint/docker-compose.yaml up --build --abort-on-container-exit --exit-code-from linter\n</code></pre> <p>Or manually:</p> <pre><code>black src/\nisort src/\nruff check src/\n</code></pre>"},{"location":"development/contributing/#4-commit-and-push","title":"4. Commit and Push","text":"<p>Use conventional commit prefixes: <code>feat:</code>, <code>fix:</code>, <code>docs:</code>, <code>test:</code>, <code>refactor:</code></p>"},{"location":"development/contributing/#5-open-a-pr","title":"5. Open a PR","text":"<p>Create a Pull Request on GitHub with a clear title and description of the change.</p>"},{"location":"development/contributing/#code-style","title":"Code Style","text":"<ul> <li>Follow PEP 8, use type hints, write Google-style docstrings</li> <li>Max line length: 80 characters, enforced by <code>black</code></li> </ul>"},{"location":"development/contributing/#license","title":"License","text":"<p>By contributing, you agree that your contributions will be licensed under the MIT License.</p>"},{"location":"development/setup/","title":"Development Setup","text":""},{"location":"development/setup/#prerequisites","title":"Prerequisites","text":"<ul> <li>Docker: Required for all development workflows</li> <li>Git: For version control</li> <li>VS Code: Optional but recommended</li> </ul>"},{"location":"development/setup/#setup-options","title":"Setup Options","text":""},{"location":"development/setup/#option-1-vs-code-dev-containers-recommended","title":"Option 1: VS Code Dev Containers (Recommended)","text":"<ol> <li>Open the project in VS Code</li> <li>Install the \"Dev Containers\" extension</li> <li>Press <code>Cmd/Ctrl + Shift + P</code> \u2192 \"Dev Containers: Reopen in Container\"</li> </ol> <p>The devcontainer includes Python 3.11, Redis, and all dependencies pre-installed.</p>"},{"location":"development/setup/#option-2-docker-compose","title":"Option 2: Docker Compose","text":"<p>Run tests: <pre><code>docker compose -f tests/docker-compose.yaml up --build --abort-on-container-exit --exit-code-from test\n</code></pre></p> <p>Reformat code: <pre><code>docker compose -f reformat/docker-compose.yaml up --build --abort-on-container-exit\n</code></pre></p> <p>Validate docs: <pre><code>docker compose -f docs-validate/docker-compose.yaml up --build --abort-on-container-exit\n</code></pre></p>"},{"location":"development/setup/#option-3-virtual-environment","title":"Option 3: Virtual Environment","text":"<pre><code>python3 -m venv .venv\nsource .venv/bin/activate  # Windows: .venv\\Scripts\\activate\n\npip install --upgrade pip\npip install -e .\npip install -e .[test]\npip install -e .[dev]\npip install -e .[docs]\n</code></pre> <p>Start a Redis or DragonflyDB backend:</p> <pre><code>docker run -d -p 6379:6379 redis:7\n</code></pre>"},{"location":"development/setup/#verifying-your-setup","title":"Verifying Your Setup","text":"<pre><code>from synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nmem = Memory()\nmem.test = \"Hello, synced-memory!\"\nprint(mem.test)  # Hello, synced-memory!\n</code></pre> <p>Run the test suite:</p> <pre><code>pytest tests/ -v\n</code></pre>"},{"location":"development/setup/#project-structure","title":"Project Structure","text":"<pre><code>synced-memory/\n\u251c\u2500\u2500 .devcontainer/          # Dev container configuration\n\u251c\u2500\u2500 .github/workflows/ci.yaml\n\u251c\u2500\u2500 .vscode/tasks.json\n\u251c\u2500\u2500 docs/                   # Documentation source\n\u251c\u2500\u2500 docs-validate/          # Docs validation Docker setup\n\u251c\u2500\u2500 lint/                   # Linting Docker setup\n\u251c\u2500\u2500 reformat/               # Code formatting Docker setup\n\u251c\u2500\u2500 scripts/                # Utility scripts\n\u251c\u2500\u2500 src/\n\u2502   \u2514\u2500\u2500 synced_memory/\n\u2502       \u251c\u2500\u2500 __init__.py       # Package re-exports\n\u2502       \u251c\u2500\u2500 common.py         # Shared business logic\n\u2502       \u251c\u2500\u2500 redis/\n\u2502       \u2502   \u2514\u2500\u2500 __init__.py   # Redis backend (thin wrapper)\n\u2502       \u2514\u2500\u2500 dragonflydb/\n\u2502           \u2514\u2500\u2500 __init__.py   # DragonflyDB backend (thin wrapper)\n\u251c\u2500\u2500 tests/\n\u2502   \u251c\u2500\u2500 test_redis.py         # Redis test suite\n\u2502   \u2514\u2500\u2500 docker-compose.yaml   # Test environment (Redis + DragonflyDB)\n\u251c\u2500\u2500 mkdocs.yml\n\u2514\u2500\u2500 pyproject.toml\n</code></pre>"},{"location":"development/setup/#common-issues","title":"Common Issues","text":"<p>Redis Connection Error: - Ensure Redis is running: <code>redis-cli ping</code> should return <code>PONG</code> - Check <code>REDIS_HOST</code> environment variable</p> <p>Import Error (<code>ModuleNotFoundError: No module named 'synced_memory'</code>): <pre><code>pip install -e .\n</code></pre></p> <p>Port already in use (6379): <pre><code>lsof -i :6379 &amp;&amp; kill -9 &lt;PID&gt;\n</code></pre></p>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10+</li> <li>Redis 7+ or DragonflyDB (local or remote)</li> <li>Docker (optional, for development)</li> </ul>"},{"location":"getting-started/installation/#install-from-pypi","title":"Install from PyPI","text":"<pre><code>pip install synced-memory\n</code></pre>"},{"location":"getting-started/installation/#install-from-source","title":"Install from Source","text":"<pre><code>git clone https://github.com/sinan-ozel/synced-memory.git\ncd synced-memory\npip install .\n</code></pre>"},{"location":"getting-started/installation/#backend-setup","title":"Backend Setup","text":"<p>synced-memory requires a Redis-compatible server. You can use Redis or DragonflyDB.</p>"},{"location":"getting-started/installation/#option-1-use-docker-recommended","title":"Option 1: Use Docker (Recommended)","text":"<p>Redis: <pre><code>docker run -d -p 6379:6379 redis:7\n</code></pre></p> <p>DragonflyDB: <pre><code>docker run -d -p 6379:6379 docker.dragonflydb.io/dragonflydb/dragonfly\n</code></pre></p>"},{"location":"getting-started/installation/#option-2-install-redis-locally","title":"Option 2: Install Redis Locally","text":"<p>macOS: <pre><code>brew install redis\nbrew services start redis\n</code></pre></p> <p>Ubuntu/Debian: <pre><code>sudo apt-get update &amp;&amp; sudo apt-get install redis-server\nsudo systemctl start redis-server\n</code></pre></p>"},{"location":"getting-started/installation/#environment-variables","title":"Environment Variables","text":"Variable Default Description <code>REDIS_HOST</code> <code>redis</code> Backend hostname <code>REDIS_PORT</code> <code>6379</code> Backend port <code>REDIS_PREFIX</code> <code>memory:</code> Key prefix for namespacing <p>Example <code>.env</code> file:</p> <pre><code>REDIS_HOST=localhost\nREDIS_PORT=6379\nREDIS_PREFIX=myapp:\n</code></pre>"},{"location":"getting-started/installation/#verify-installation","title":"Verify Installation","text":"<pre><code>from synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nmem = Memory()\nmem.test = \"Hello, synced-memory!\"\nprint(mem.test)  # Hello, synced-memory!\n</code></pre>"},{"location":"getting-started/quickstart/","title":"Quickstart","text":"<p>This guide will get you up and running with synced-memory in minutes.</p>"},{"location":"getting-started/quickstart/#choose-a-backend","title":"Choose a Backend","text":"<p>Import from the backend you want to use:</p> <pre><code>from synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n</code></pre> <p>Both backends expose the same API.</p>"},{"location":"getting-started/quickstart/#basic-usage","title":"Basic Usage","text":"<pre><code>from synced_memory.redis import Memory\n\nmem = Memory()\nmem.counter = 0\nmem.username = \"alice\"\nmem.settings = {\"theme\": \"dark\", \"notifications\": True}\n\nprint(mem.counter)    # 0\nprint(mem.username)   # alice\nprint(mem.settings)   # {'theme': 'dark', 'notifications': True}\n</code></pre>"},{"location":"getting-started/quickstart/#cross-process-sharing","title":"Cross-Process Sharing","text":"<pre><code># Process 1\nfrom synced_memory.redis import Memory\n\nmem = Memory()\nmem.shared_value = \"Hello from Process 1\"\n</code></pre> <pre><code># Process 2 (different Python process)\nfrom synced_memory.redis import Memory\n\nmem = Memory()\nprint(mem.shared_value)  # \"Hello from Process 1\"\n</code></pre>"},{"location":"getting-started/quickstart/#context-manager","title":"Context Manager","text":"<pre><code>with Memory() as memory:\n    memory.session = \"active\"\n    print(memory.session)  # \"active\"\n\n# Later, in another process:\nwith Memory() as memory:\n    print(memory.session)  # \"active\"\n</code></pre>"},{"location":"getting-started/quickstart/#auto-syncing-collections","title":"Auto-Syncing Collections","text":"<p>Lists and dictionaries automatically sync to the backend:</p> <pre><code>mem = Memory()\n\nmem.tasks = [\"task1\", \"task2\"]\nmem.tasks.append(\"task3\")  # Automatically syncs\n\nmem.config = {\"lang\": \"en\"}\nmem.config[\"theme\"] = \"dark\"  # Automatically syncs\n\nmem.data = {\"user\": {\"name\": \"Alice\", \"age\": 30}}\nmem.data[\"user\"][\"age\"] = 31  # Syncs the entire structure\n</code></pre>"},{"location":"getting-started/quickstart/#converting-to-plain-types","title":"Converting to Plain Types","text":"<p>When you need regular Python objects (for serialization, pickling, etc.):</p> <pre><code>mem.items = [1, 2, 3]\nplain_list = mem.items.aslist()  # Returns: [1, 2, 3]\n\nmem.config = {\"key\": \"value\"}\nplain_dict = mem.config.asdict()  # Returns: {'key': 'value'}\n\nimport pickle\npickle.dump(plain_list, open('data.pkl', 'wb'))\n</code></pre>"},{"location":"getting-started/quickstart/#next-steps","title":"Next Steps","text":"<ul> <li>Learn about basic usage patterns</li> <li>Explore auto-synced collections</li> <li>Set up namespacing for multi-tenant apps</li> <li>Use agent memory for conversational AI</li> </ul>"},{"location":"guide/agents/","title":"Scoped Memory","text":"<p>synced-memory provides <code>PrefixedMemory</code> to isolate memory by a custom scope prefix \u2014 multiple scopes can share the same backend safely.</p>"},{"location":"guide/agents/#prefixedmemory-class","title":"PrefixedMemory Class","text":"<pre><code>from synced_memory.redis import PrefixedMemory      # Redis\nfrom synced_memory.dragonflydb import PrefixedMemory  # DragonflyDB\n\nmem = PrefixedMemory(prefix=\"session_abc\")\n</code></pre>"},{"location":"guide/agents/#basic-usage","title":"Basic Usage","text":"<pre><code>from synced_memory.redis import PrefixedMemory      # Redis\nfrom synced_memory.dragonflydb import PrefixedMemory  # DragonflyDB\n\nmem = PrefixedMemory(prefix=\"user_123\")\n\nmem.messages = [\n    {\"role\": \"user\", \"content\": \"Hello!\"},\n    {\"role\": \"assistant\", \"content\": \"Hi! How can I help you?\"}\n]\nmem.user_name = \"Alice\"\n</code></pre>"},{"location":"guide/agents/#accessing-from-different-processes","title":"Accessing from Different Processes","text":"<pre><code># Process 1\nmem = PrefixedMemory(prefix=\"scope_123\")\nmem.messages.append({\"role\": \"user\", \"content\": \"What's the weather?\"})\n\n# Process 2 (different worker/pod)\nmem2 = PrefixedMemory(prefix=\"scope_123\")\nprint(mem2.messages)  # includes the message from Process 1\n</code></pre>"},{"location":"guide/agents/#integration-with-llm-frameworks","title":"Integration with LLM Frameworks","text":""},{"location":"guide/agents/#litellm","title":"LiteLLM","text":"<pre><code>from synced_memory.redis import PrefixedMemory      # Redis\nfrom synced_memory.dragonflydb import PrefixedMemory  # DragonflyDB\nimport litellm\n\nmem = PrefixedMemory(prefix=\"session_xyz\")\n\nif not hasattr(mem, 'messages'):\n    mem.messages = []\n\nmem.messages.append({\"role\": \"user\", \"content\": \"Hello!\"})\n\nresponse = litellm.completion(\n    model=\"gpt-4o\",\n    messages=mem.messages.aslist()  # pass plain list to LiteLLM\n)\n\nmem.messages.append({\n    \"role\": \"assistant\",\n    \"content\": response.choices[0].message.content\n})\n</code></pre>"},{"location":"guide/agents/#multi-agent-systems","title":"Multi-Agent Systems","text":"<pre><code>from synced_memory.redis import PrefixedMemory      # Redis\nfrom synced_memory.dragonflydb import PrefixedMemory  # DragonflyDB\n\n# Agent 1: Intent Classifier\nmem = PrefixedMemory(prefix=\"multi_agent_session\")\nmem.detected_intent = \"technical_support\"\n\n# Agent 2: Router (different process)\nmem2 = PrefixedMemory(prefix=\"multi_agent_session\")\nif mem2.detected_intent == \"technical_support\":\n    mem2.assigned_specialist = \"tech_agent_3\"\n\n# Agent 3: Specialist (different process)\nmem3 = PrefixedMemory(prefix=\"multi_agent_session\")\nif hasattr(mem3, 'assigned_specialist'):\n    mem3.resolution_status = \"in_progress\"\n</code></pre>"},{"location":"guide/agents/#best-practices","title":"Best Practices","text":"<ol> <li>Use unique prefixes (UUIDs, session tokens, or user IDs)</li> <li>Pass <code>.aslist()</code> / <code>.asdict()</code> to LLM libraries that may <code>deepcopy</code> the data</li> <li>Trim message history to manage context window limits</li> <li>Clean up old scopes to avoid unbounded backend growth</li> </ol>"},{"location":"guide/basic-usage/","title":"Basic Usage","text":""},{"location":"guide/basic-usage/#creating-a-memory-instance","title":"Creating a Memory Instance","text":"<pre><code>from synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nmem = Memory()\n</code></pre> <p>By default, <code>Memory</code> connects to <code>redis:6379</code> (or <code>dragonflydb:6379</code> for the DragonflyDB backend) and uses the prefix <code>memory:</code>.</p>"},{"location":"guide/basic-usage/#setting-values","title":"Setting Values","text":"<pre><code>mem.counter = 42\nmem.name = \"Alice\"\nmem.active = True\nmem.score = 98.5\nmem.items = [1, 2, 3]\nmem.config = {\"theme\": \"dark\", \"lang\": \"en\"}\n</code></pre> <p>All values are JSON-serialized and written to the backend.</p>"},{"location":"guide/basic-usage/#getting-values","title":"Getting Values","text":"<pre><code>print(mem.counter)  # 42\nprint(mem.name)     # Alice\nprint(mem.active)   # True\nprint(mem.items)    # [1, 2, 3]\n</code></pre>"},{"location":"guide/basic-usage/#checking-if-attributes-exist","title":"Checking if Attributes Exist","text":"<pre><code>if hasattr(mem, 'counter'):\n    print(f\"Counter exists: {mem.counter}\")\n\ntry:\n    value = mem.nonexistent\nexcept AttributeError:\n    print(\"Attribute doesn't exist\")\n</code></pre>"},{"location":"guide/basic-usage/#deleting-values","title":"Deleting Values","text":"<pre><code>mem.temp = \"temporary\"\ndel mem.temp\n# Now mem.temp raises AttributeError\n</code></pre>"},{"location":"guide/basic-usage/#working-with-complex-data","title":"Working with Complex Data","text":"<pre><code>mem.user = {\n    \"name\": \"Alice\",\n    \"profile\": {\n        \"age\": 30,\n        \"preferences\": {\n            \"theme\": \"dark\",\n            \"notifications\": [\"email\", \"push\"]\n        }\n    }\n}\n\nprint(mem.user[\"profile\"][\"age\"])  # 30\n</code></pre>"},{"location":"guide/basic-usage/#persistence","title":"Persistence","text":"<p>Values persist across program restarts:</p> <pre><code># Run 1\nmem = Memory()\nmem.persistent_value = \"I will survive!\"\n\n# Later, after program restart\nmem = Memory()\nprint(mem.persistent_value)  # \"I will survive!\"\n</code></pre>"},{"location":"guide/basic-usage/#multiprocessing-safety","title":"Multiprocessing Safety","text":"<pre><code>from multiprocessing import Process\nfrom synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\ndef worker(worker_id):\n    mem = Memory()\n    print(f\"Worker {worker_id} sees: {mem.shared_value}\")\n\nmem = Memory()\nmem.shared_value = \"hello\"\n\nprocesses = [Process(target=worker, args=(i,)) for i in range(5)]\nfor p in processes:\n    p.start()\nfor p in processes:\n    p.join()\n</code></pre>"},{"location":"guide/basic-usage/#redis-connection-resilience","title":"Redis Connection Resilience","text":"<p>If the backend is temporarily unavailable, changes are queued locally and flushed automatically when the connection is restored:</p> <pre><code>mem = Memory()\nmem.offline_value = \"queued\"  # Queued locally if backend is down\n# When backend comes back online, the change is synced automatically\n</code></pre>"},{"location":"guide/collections/","title":"Auto-Synced Collections","text":"<p>synced-memory automatically wraps lists and dictionaries in <code>SyncedList</code> and <code>SyncedDict</code>. Supported mutations on these wrappers are immediately written back to the backend.</p>"},{"location":"guide/collections/#auto-synced-lists-syncedlist","title":"Auto-Synced Lists (<code>SyncedList</code>)","text":""},{"location":"guide/collections/#supported-operations","title":"Supported Operations","text":"<p>The following operations sync automatically:</p> Operation Example <code>append(item)</code> <code>mem.tasks.append(\"new\")</code> <code>extend(iterable)</code> <code>mem.tasks.extend([\"a\", \"b\"])</code> <code>insert(index, item)</code> <code>mem.tasks.insert(0, \"first\")</code> <code>remove(item)</code> <code>mem.tasks.remove(\"done\")</code> <code>pop([index])</code> <code>mem.tasks.pop()</code> <pre><code>from synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nmem = Memory()\nmem.tasks = [1, 2, 3]\n\nmem.tasks.append(4)       # [1, 2, 3, 4]\nmem.tasks.extend([5, 6])  # [1, 2, 3, 4, 5, 6]\nmem.tasks.insert(0, 0)    # [0, 1, 2, 3, 4, 5, 6]\nmem.tasks.remove(0)       # [1, 2, 3, 4, 5, 6]\nmem.tasks.pop()           # [1, 2, 3, 4, 5]\n\nmem2 = Memory()\nprint(mem2.tasks)  # [1, 2, 3, 4, 5]\n</code></pre> <p>Unsupported in-place operations</p> <p>Operations not listed above (e.g. <code>sort</code>, <code>reverse</code>, index assignment <code>mem.items[0] = x</code>) are inherited from <code>list</code> but do not sync. Re-assign the whole attribute after such operations:</p> <pre><code># Does NOT sync:\nmem.items.pop()\n\n# Use this instead:\nplain = mem.items.aslist()\nplain.pop()\nmem.items = plain  # syncs\n</code></pre>"},{"location":"guide/collections/#auto-synced-dictionaries-synceddict","title":"Auto-Synced Dictionaries (<code>SyncedDict</code>)","text":""},{"location":"guide/collections/#supported-operations_1","title":"Supported Operations","text":"Operation Example <code>__setitem__(key, value)</code> <code>mem.config[\"key\"] = \"val\"</code> <code>update(other)</code> <code>mem.config.update({\"a\": 1})</code> <code>pop(key[, default])</code> <code>mem.config.pop(\"key\")</code> <pre><code>mem = Memory()\nmem.config = {\"theme\": \"dark\", \"lang\": \"en\"}\n\nmem.config[\"notifications\"] = True\nmem.config.update({\"font\": \"Arial\", \"size\": 12})\n\nmem2 = Memory()\nprint(mem2.config)\n# {'theme': 'dark', 'lang': 'en', 'notifications': True, 'font': 'Arial', 'size': 12}\n</code></pre> <p>Unsupported in-place operations</p> <p><code>pop</code>, <code>popitem</code>, <code>clear</code>, <code>setdefault</code>, and <code>del mem.config[\"key\"]</code> are inherited from <code>dict</code> but do not sync. Re-assign the whole attribute after such operations.</p>"},{"location":"guide/collections/#nested-collections","title":"Nested Collections","text":"<p>synced-memory handles deeply nested structures \u2014 any mutation at any level syncs the top-level attribute:</p> <pre><code>mem = Memory()\nmem.data = {\"users\": [{\"name\": \"Alice\", \"scores\": [95, 87]}]}\n\nmem.data[\"users\"][0][\"scores\"].append(92)  # syncs\n\nmem2 = Memory()\nprint(mem2.data[\"users\"][0][\"scores\"])  # [95, 87, 92]\n</code></pre>"},{"location":"guide/collections/#converting-to-plain-python-types","title":"Converting to Plain Python Types","text":"<p>Use <code>.aslist()</code> and <code>.asdict()</code> when you need regular Python objects:</p> <pre><code>mem.items = [1, 2, 3]\nplain_list = mem.items.aslist()  # plain list, recursively converted\n\nmem.config = {\"key\": \"value\", \"nested\": {\"a\": 1}}\nplain_dict = mem.config.asdict()  # plain dict, recursively converted\n\nimport pickle\npickle.dump(plain_list, open('data.pkl', 'wb'))  # works\n</code></pre> <p>Both methods convert nested <code>SyncedList</code>/<code>SyncedDict</code> objects recursively.</p>"},{"location":"guide/collections/#type-checking","title":"Type Checking","text":"<pre><code>from synced_memory.redis import Memory\nfrom synced_memory.common import SyncedList, SyncedDict\n\nmem = Memory()\nmem.items = [1, 2, 3]\nmem.config = {\"key\": \"value\"}\n\nisinstance(mem.items, SyncedList)   # True\nisinstance(mem.config, SyncedDict)  # True\n\nplain = mem.items.aslist()\nisinstance(plain, list)        # True\nisinstance(plain, SyncedList)  # False\n</code></pre>"},{"location":"guide/context-management/","title":"Context Management","text":"<p>synced-memory supports Python's context manager protocol.</p>"},{"location":"guide/context-management/#basic-usage","title":"Basic Usage","text":"<pre><code>from synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nwith Memory() as memory:\n    memory.session = \"active\"\n    memory.user_id = 12345\n    print(memory.session)  # \"active\"\n</code></pre> <p>When the <code>with</code> block exits, any remaining queued writes are flushed and the background thread is stopped cleanly.</p>"},{"location":"guide/context-management/#persistence-across-contexts","title":"Persistence Across Contexts","text":"<p>Values written inside a context block persist in the backend and are visible to any subsequent instance:</p> <pre><code>with Memory() as memory:\n    memory.test_key = \"hello world\"\n\nwith Memory() as memory:\n    print(memory.test_key)  # \"hello world\"\n</code></pre>"},{"location":"guide/context-management/#exception-safety","title":"Exception Safety","text":"<p>Cleanup runs even if an exception is raised:</p> <pre><code>try:\n    with Memory() as memory:\n        memory.status = \"processing\"\n        result = risky_operation()\n        memory.status = \"completed\"\nexcept Exception as e:\n    print(f\"Operation failed: {e}\")\n    # memory is properly cleaned up\n</code></pre>"},{"location":"guide/context-management/#combining-with-other-context-managers","title":"Combining with Other Context Managers","text":"<pre><code>from synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nwith Memory() as memory, open('log.txt', 'w') as log_file:\n    memory.session_id = \"abc123\"\n    log_file.write(f\"Session: {memory.session_id}\\n\")\n</code></pre>"},{"location":"guide/context-management/#without-context-managers","title":"Without Context Managers","text":"<p>Context managers are optional. Direct instantiation works fine:</p> <pre><code>mem = Memory()\nmem.value = \"test\"\n# Memory object lives until garbage collected\n</code></pre>"},{"location":"guide/context-management/#async-code","title":"Async Code","text":"<p>Memory operations are synchronous (blocking). Use them outside of async-critical paths or run them in a thread pool:</p> <pre><code>import asyncio\nfrom synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nasync def async_task():\n    with Memory() as memory:\n        memory.task_status = \"running\"\n        await asyncio.sleep(1)\n        memory.task_status = \"completed\"\n\nasyncio.run(async_task())\n</code></pre>"},{"location":"guide/namespacing/","title":"Namespacing","text":"<p>synced-memory supports namespacing through key prefixes, allowing multiple applications or tenants to share the same backend without key collisions.</p>"},{"location":"guide/namespacing/#default-prefix","title":"Default Prefix","text":"<p>By default the prefix is <code>memory:</code>:</p> <pre><code>from synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nmem = Memory()\nmem.counter = 1\n# Stored in backend as: memory:counter\n</code></pre>"},{"location":"guide/namespacing/#custom-prefix-via-environment-variable","title":"Custom Prefix via Environment Variable","text":"<pre><code>export REDIS_PREFIX=\"myapp:\"\n</code></pre> <pre><code>mem = Memory()\nmem.counter = 1\n# Stored as: myapp:counter\n</code></pre>"},{"location":"guide/namespacing/#custom-prefix-via-constructor","title":"Custom Prefix via Constructor","text":"<p>Pass <code>db_prefix</code> directly:</p> <pre><code>mem = Memory(db_prefix=\"session_123:\")\nmem.value = \"hello\"\n# Stored as: session_123:value\n</code></pre>"},{"location":"guide/namespacing/#multi-tenant-applications","title":"Multi-Tenant Applications","text":"<pre><code>from synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nmem1 = Memory(db_prefix=\"tenant1:\")\nmem1.data = \"tenant 1 data\"\n\nmem2 = Memory(db_prefix=\"tenant2:\")\nmem2.data = \"tenant 2 data\"\n\nprint(mem1.data)  # \"tenant 1 data\"\nprint(mem2.data)  # \"tenant 2 data\"\n</code></pre>"},{"location":"guide/namespacing/#environment-based-prefixes","title":"Environment-Based Prefixes","text":"<pre><code>import os\nfrom synced_memory.redis import Memory      # Redis\nfrom synced_memory.dragonflydb import Memory  # DragonflyDB\n\nenv = os.getenv('ENVIRONMENT', 'development')\nmem = Memory(db_prefix=f'{env}:')\nmem.config = {\"env\": env}\n# development:config, staging:config, production:config\n</code></pre>"},{"location":"guide/namespacing/#inspecting-keys","title":"Inspecting Keys","text":"<pre><code>redis-cli KEYS \"myapp:*\"\n</code></pre> <pre><code>import redis\n\nr = redis.Redis(host='localhost', port=6379)\nkeys = r.keys('myapp:*')\nfor key in keys:\n    print(key.decode('utf-8'))\n</code></pre>"},{"location":"guide/namespacing/#best-practices","title":"Best Practices","text":"<ol> <li>Use descriptive prefixes: <code>myapp:prod:</code> is better than <code>mp:</code></li> <li>Include environment: <code>myapp:dev:</code>, <code>myapp:prod:</code></li> <li>End with a colon: makes individual keys more readable</li> <li>Avoid special characters beyond <code>:</code> and <code>_</code></li> </ol>"}]}