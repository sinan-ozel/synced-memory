services:
  dev:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace
    environment:
      - PYTHONPATH=/workspace/src
    command: ["tail", "-f", "/dev/null"]
