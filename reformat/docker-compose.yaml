services:
  reformat:
    build:
      context: ..
      dockerfile: reformat/Dockerfile
    container_name: reformat-code
    volumes:
      - ${HOST_PROJECT_PATH:-..}:/workspace/src:rw
    working_dir: /workspace
    network_mode: bridge
